wcss = []
metrics = ["manhattan", "euclidean", "cosine"]
acc = []

for metric in metrics:
    for j in range(2, 7):
        reduced_data = PCA(n_components=j).fit_transform(dataScaled)
        
        for i in range(1, 11): 
            kMedoids = KMedoids(metric=metric, n_clusters=i, random_state=42)
            
            predict = kMedoids.fit_predict(reduced_data)
            
            if len(set(predict)) > 1:
                silhouette_avg = silhouette_score(reduced_data, predict)
                acc.append((metric, i, silhouette_avg))

acc

max_value = max(acc, key=lambda x: x[2])
max_value